import spacy
    def updateFunc(new_values, running_value):
        update_value = [
        if not running_value:
            running_value = [
        return update_value
nlp = spacy.load('en_core_web_sm')
text = "Kerry Taste and Nutrition Ingredients and Expertise | Kerry"
about_doc = nlp(text)
sentences = list(about_doc.sents)
for sentence in sentences:
    print (f"{sentence}\n")